<figure class="about-look" data-about-look>
  <div
    class="about-look-stage"
    data-about-look-stage
    role="slider"
    tabindex="0"
    aria-label="Séquence interactive : déplacez le curseur pour faire varier le regard"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-valuenow="50"
    aria-valuetext="Position médiane"
  >
    <video
      class="about-look-video"
      data-about-look-video
      muted
      playsinline
      preload="metadata"
      poster="/media/identity/nicolas-jez-about.webp"
      disablepictureinpicture
      aria-hidden="true"
    >
      <source src="/media/identity/nicolas-look-reference-v1.mp4" type="video/mp4" />
    </video>
    <span class="about-look-index" aria-hidden="true">REGARD / 01</span>
    <span class="about-look-status" aria-hidden="true">Déplacez le curseur</span>
  </div>
  <figcaption>Déplacez la souris : le regard vous suit.</figcaption>
</figure>

<script>
  import { initAboutLookVideo } from '../../lib/about-look';
  initAboutLookVideo();
  document.addEventListener('astro:page-load', initAboutLookVideo);
</script>

<style>
  .about-look {
    width: fit-content;
    max-width: 100%;
    margin: var(--space-7) 0 0;
  }

  .about-look-stage {
    position: relative;
    isolation: isolate;
    width: auto;
    height: min(72svh, 42rem);
    aspect-ratio: 9 / 16;
    max-width: 100%;
    overflow: hidden;
    cursor: ew-resize;
    outline: none;
  }

  .about-look-stage::after {
    position: absolute;
    z-index: 1;
    inset: 0;
    pointer-events: none;
    content: '';
    background: linear-gradient(180deg, rgba(5, 7, 8, 0.2), transparent 34%, rgba(5, 7, 8, 0.24));
    mix-blend-mode: multiply;
  }

  .about-look-stage:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 6px;
  }

  .about-look-video {
    position: absolute;
    z-index: 0;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    opacity: 0;
    transition: opacity 360ms var(--ease-out);
  }

  .about-look[data-ready='true'] .about-look-video {
    opacity: 1;
  }

  .about-look-index,
  .about-look-status {
    position: absolute;
    z-index: 2;
    color: rgba(245, 238, 232, 0.76);
    font-family: var(--font-mono);
    font-size: 0.6rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .about-look-index {
    top: 1.1rem;
    left: 1.15rem;
  }
  .about-look-status {
    right: 1.15rem;
    bottom: 1.05rem;
  }

  .about-look figcaption {
    margin-top: var(--space-3);
    color: var(--color-muted);
    font-size: 0.72rem;
    line-height: var(--leading-relaxed);
  }

  @media (max-width: 60rem) {
    .about-look-stage {
      height: min(64svh, 36rem);
    }
  }

  @media (max-width: 48rem) {
    .about-look {
      margin-top: var(--space-6);
    }
    .about-look-stage {
      height: min(70svh, 34rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .about-look-video {
      transition: none;
    }
  }
</style>
