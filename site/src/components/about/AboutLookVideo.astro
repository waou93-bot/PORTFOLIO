<section class="section about-look" aria-labelledby="about-look-title">
  <div class="container about-look-grid">
    <div class="about-look-copy">
      <p class="eyebrow">Présence</p>
      <h2 id="about-look-title" class="about-look-title">Le regard suit l’attention.</h2>
      <p class="about-text">
        La séquence reste immobile jusqu’à votre geste. Déplacez le curseur pour parcourir le
        mouvement du regard, sans lecture automatique ni boucle vidéo.
      </p>
      <p class="about-look-instruction">Déplacez la souris <span aria-hidden="true">↔</span> le regard suit</p>
    </div>

    <figure class="about-look-figure" data-about-look>
      <div
        class="about-look-stage"
        data-about-look-stage
        role="group"
        tabindex="0"
        aria-label="Séquence interactive : déplacez le curseur pour faire varier le regard"
      >
        <video
          class="about-look-video"
          data-about-look-video
          muted
          playsinline
          preload="metadata"
          poster="/media/identity/nicolas-jez-about.webp"
          disablepictureinpicture
          aria-hidden="true"
        >
          <source src="/media/identity/nicolas-look-reference-v1.mp4" type="video/mp4" />
        </video>
        <span class="about-look-index" aria-hidden="true">REGARD / 01</span>
        <span class="about-look-status" aria-hidden="true">Déplacez le curseur</span>
      </div>
      <figcaption>Une séquence extraite de la vidéo de référence, transformée en matière interactive.</figcaption>
    </figure>
  </div>
</section>

<script>
  import { initAboutLookVideo } from '../../lib/about-look';
  initAboutLookVideo();
  document.addEventListener('astro:page-load', initAboutLookVideo);
</script>

<style>
  .about-look { overflow: hidden; }

  .about-look-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr);
    align-items: center;
    gap: var(--space-10);
  }

  .about-look-copy {
    display: grid;
    align-content: center;
    gap: var(--space-5);
    max-width: 34rem;
  }

  .about-look-title {
    max-width: 10ch;
    font-size: clamp(2.1rem, 5vw, 4.8rem);
    letter-spacing: -0.065em;
  }

  .about-look-instruction {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    gap: 0.55rem;
    margin: 0;
    padding-top: var(--space-4);
    border-top: var(--border-1);
    color: var(--color-ink-soft);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .about-look-instruction span {
    color: var(--color-accent);
    font-size: 1rem;
  }

  .about-look-figure {
    min-width: 0;
    margin: 0;
    justify-self: end;
  }

  .about-look-stage {
    position: relative;
    isolation: isolate;
    width: auto;
    height: min(72svh, 42rem);
    aspect-ratio: 9 / 16;
    max-width: 100%;
    overflow: hidden;
    cursor: ew-resize;
    outline: none;
  }

  .about-look-stage::after {
    position: absolute;
    z-index: 1;
    inset: 0;
    pointer-events: none;
    content: '';
    background: linear-gradient(180deg, rgba(5, 7, 8, 0.2), transparent 34%, rgba(5, 7, 8, 0.24));
    mix-blend-mode: multiply;
  }

  .about-look-stage:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 6px;
  }

  .about-look-video {
    position: absolute;
    z-index: 0;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    opacity: 0;
    transition: opacity 360ms var(--ease-out);
  }

  .about-look-figure[data-ready='true'] .about-look-video { opacity: 1; }

  .about-look-index,
  .about-look-status {
    position: absolute;
    z-index: 2;
    color: rgba(245, 238, 232, 0.76);
    font-family: var(--font-mono);
    font-size: 0.6rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .about-look-index { top: 1.1rem; left: 1.15rem; }
  .about-look-status { right: 1.15rem; bottom: 1.05rem; }

  .about-look-figure figcaption {
    margin-top: var(--space-3);
    color: var(--color-muted);
    font-size: 0.72rem;
    line-height: var(--leading-relaxed);
  }

  @media (max-width: 60rem) {
    .about-look-grid { gap: var(--space-8); }
    .about-look-title { font-size: clamp(2rem, 6vw, 3.8rem); }
  }

  @media (max-width: 48rem) {
    .about-look-grid { grid-template-columns: 1fr; }
    .about-look-copy { max-width: 38rem; }
    .about-look-title { max-width: 13ch; }
    .about-look-figure { justify-self: center; }
    .about-look-stage { height: min(70svh, 34rem); }
  }

  @media (prefers-reduced-motion: reduce) {
    .about-look-video { transition: none; }
  }
</style>
